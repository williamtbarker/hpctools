# Contributing

Keep commands portable to Bash 3.2, quote paths, and preserve dry-run defaults for scheduler
mutations. Add a mocked integration test for every behavior change.

```bash
brew install shellcheck  # macOS
./scripts/verify.sh
```

Do not add organization-specific partitions, accounts, hosts, paths, or credentials to examples.
